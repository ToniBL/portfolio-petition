<h1>this is the petition Site</h1>

<h2>Welcome to petition XY </h2>
        <form id="form" method='POST' style="display: flex; flex-direction: column; justify-content: space-between; width: 40%; height: 50%;">
            <input type='text' name='firstname' placeholder='First Name' autocomplete='off'>
            <input type='text' name='lastname' placeholder='Last Name' autocomplete='off'>
            <div class="canvas">
                <input name="signature" id="inputfield" type="hidden"> <label for="signature">Signature</label><canvas id="signature" width="500" height="300"></canvas>
            </div>
             <input type="hidden" name="_csrf" value="{{csrfToken}}">
                        <button> submit </button>
        </form>

      {{#if errInAddSupporter}} <p>Whoops! Something went wrong, please try again</p> {{/if}}